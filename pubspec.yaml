name: flutter_login_template
description: A Flutter package to create set of pages are used for sign in/up workflow. It will be useful for your awesome app.
version: 1.1.0
homepage: https://romanticdeveloper.com
repository: https://github.com/minhhung2556/romantic_project_flutter_login_template

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: